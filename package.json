{
  "name": "binance-stablecoin-route-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live stablecoin route comparison and guarded Convert execution built with Binance Agent OS",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "analyze": "node src/cli.js",
    "verify": "node --check src/server.js && node --check src/agent-os.js && node --check src/api-convert.js && node --check public/app.js && node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@binance/binance-cli": "1.3.0"
  }
}
